export interface MetaData {
    page: number;
    // pageSize: number;
    // firstPage: number;
    // lastPage: number;
    // previousPage: number;
    totalRecords: number;
    // nextPage?: number;
}
